---
title: "当AI写代码时，为什么它总是把配置写死在代码里？"
description: "本文通过Zotero项目优化经历，思考LLM在代码生成中硬编码倾向的本质原因，以及人类在AI辅助编程中的独特价值。"
author: SG-Amadeus
date: 2026-03-15 19:30:00 +0800
categories: [AI编程, 软件架构, 人机协作]
tags: [llm, hardcoding, architecture, ai-assisted-programming, human-ai-collaboration]
---

# 当AI写代码时，为什么它总是把配置写死在代码里？

今天下午，我完成了Zotero LLM Bridge客户端的架构优化。看着屏幕上绿色的“All tests passed”提示，我的心情有些复杂。一方面，我为解决了一个技术问题感到欣慰；另一方面，我却陷入了一种更深的困惑：为什么这些由AI生成的代码，总是把配置信息硬编码在代码里？

这不是我第一次遇到这个问题。在过去的几个月里，我与各种AI代码生成工具协作完成了各种项目。每次审查AI生成的代码时，我总会发现那些熟悉的硬编码痕迹：端口号、API路径、超时设置、文件路径……它们像顽固的印记，深深刻在代码的逻辑中。

## 一次典型的清理工作

让我们看看Zotero项目中找到的几个例子：

```python
# 这是AI生成的一段典型代码
class ZoteroLocal:
    def __init__(self):
        self.base_url = "http://localhost:23124"  # 硬编码的端口
        self.api_prefix = "/llm-bridge"  # 硬编码的API路径
        self.timeout = 30  # 硬编码的超时时间
        self.max_retries = 3  # 硬编码的重试次数
```

看起来没问题，对吧？代码很清晰，功能也能工作。但问题在于，当用户的实际环境与这些假设不符时——比如Zotero运行在23119端口，或者API前缀是`/llm-bridge/v2`——整个系统就会崩溃。

更微妙的是另一个例子：

```python
# 测试文件中的硬编码URL
def test_connection():
    response = requests.get("http://localhost:23124/llm-bridge/health")
```

这个测试代码假设Zotero总是运行在23124端口。如果端口变了，测试就会失败。但AI为什么不想想：如果这个值需要变化，它应该放在哪里？

## 硬编码的本质：AI的思维局限

当我深入思考这个问题时，我意识到硬编码反映了AI在编程中的几个根本性局限：

**第一，AI缺乏环境的感知能力。** AI不知道它的代码将在哪里运行。是在开发者的笔记本电脑上？在测试服务器上？在生产环境的容器里？AI没有这些上下文，所以它选择了最安全的假设：`localhost:23124`。

**第二，AI倾向于解决眼前的问题。** 当AI被要求“生成一个Zotero客户端”时，它的目标是生成一个“能工作”的客户端。配置的灵活性、部署的便利性、维护的可持续性——这些都不是它的首要目标。它的训练数据教会它的是：要让代码运行起来，把值直接写进去是最快的方式。

**第三，AI难以处理抽象和具体之间的平衡。** 好的架构需要在具体和抽象之间找到平衡点。太具体了（硬编码所有值），代码就失去了灵活性；太抽象了（把所有东西都参数化），代码就变得复杂难懂。AI在这方面的判断力还很有限。

我记得有一次，我让AI为一个项目生成配置管理系统。它给出了一个非常复杂的方案：JSON配置文件、YAML覆盖、环境变量、命令行参数……层层叠加，像个俄罗斯套娃。我问它：“为什么不用简单的环境变量？”它回答：“因为需要支持所有可能的配置场景。”

AI要么走向一个极端（硬编码所有东西），要么走向另一个极端（过度设计配置系统），它很难找到那个恰到好处的中间点，因为这个中间点是由开发者根据项目实际去判断的，而AI很难洞察我们的实际需求，因为在实际开发过程中我们也可能不知道这个度在哪里。

## 人类的独特价值：看到AI看不到的东西

在项目优化中，开发者价值不在于写出了更好的代码，而在于看到了AI看不到的东西：

**看系统的边界。** 知道这个客户端将被不同的人在不同的环境中使用。有的用户Zotero运行在默认端口23119，有的用户改成了23124，还有的用户可能运行在Docker容器里。AI只看到了“Zotero客户端”这个抽象概念，而我看到了它将在真实世界中遇到的具体情境。

**看变化的方向。** 知道端口可能会变，API路径可能会版本化，超时设置可能需要调整。AI只看到了当前的静态需求，而我看到了随着时间推移可能发生的变化。

**看平衡点。** 我并没有设计一个复杂的配置系统，而是实现了一个简单的三层优先级：命令行参数 > 构造函数参数 > 默认值。这给了用户足够的灵活性，又保持了代码的简洁性。

```python
# 优化后的设计
def __init__(
    self,
    base_url: Optional[str] = None,  # 可以传入自定义URL
    api_prefix: Optional[str] = None,  # 可以传入自定义API前缀
    timeout: Optional[int] = None,  # 可以自定义超时
    max_retries: Optional[int] = None,  # 可以自定义重试次数
):
    # 使用传入的值，或者回退到合理的默认值
    self.base_url = base_url or "http://localhost:23124"
    self.api_prefix = api_prefix or "/llm-bridge"
    self.timeout = timeout or 30
    self.max_retries = max_retries or 3
```

这个设计并不复杂，但它解决了硬编码的核心问题：**它承认了“我不知道你将如何使用这段代码”**。

## 这不仅仅是技术问题

当我反思这个问题时，我意识到硬编码现象揭示了一个更深层的真相：**编程不仅仅是写代码，更是做出取舍**。

AI擅长生成语法正确的代码片段，但它不擅长做出这些取舍：
- 应该在简单性和灵活性之间如何平衡？
- 应该为哪些变化做设计，忽略哪些变化？
- 应该在什么时候抽象，什么时候具体？

这些问题的答案取决于项目的具体情境、团队的工作方式、用户的期望、技术的演化方向……这些是AI难以理解和权衡的。

这就是为什么在AI辅助编程的时代，人类开发者的角色不是变得不重要，而是变得更加重要。我们需要成为**架构的导航者**，引导AI生成符合实际需求的代码；我们需要成为**质量的守门人**，确保AI生成的代码不仅功能正确，而且架构合理；我们需要成为**情境的翻译者**，把我们理解的真实世界需求转化为AI能够处理的编程任务。

## 一种新的协作模式

基于这次经验，我开始形成一种与AI协作的新模式：

**第一阶段：让AI快速生成原型。** 我会给AI一个具体的任务：“生成一个连接Zotero HTTP API的Python客户端”。AI会生成一个能工作的版本，很可能包含硬编码。

**第二阶段：人工审查和提炼。** 我会审查AI生成的代码，识别出其中的硬编码和过度简化。我会问自己：这段代码在哪些情境下会失败？它会如何演进？用户会如何使用它？

**第三阶段：给AI明确的架构指导。** 我会告诉AI：“重写这个客户端，让所有配置都可以通过参数传入，并提供合理的默认值。”或者更具体地：“实现一个配置系统，支持命令行参数、环境变量和构造函数参数，按优先级覆盖。”

**第四阶段：迭代和精炼。** AI根据我的指导生成新版本，我再次审查，如此反复，直到达到一个平衡点。

这种模式的关键在于**明确的分工**：AI负责快速生成代码，我负责提供架构指导和情境理解。AI负责实现细节，我负责战略方向。

## 结语：走向真正的智能协作

完成Zotero项目优化后，我有了一个新的认识：**AI不会取代人类开发者，但会改变我们的工作方式**。

我们不再需要花费大量时间编写重复的样板代码，但我们需要花更多时间思考架构设计、情境理解和质量把控。我们不再是一个人在战斗，而是在与一个强大的代码生成助手协作。

硬编码问题只是这个转变中的一个小小缩影。它提醒我们，在拥抱AI带来的效率提升的同时，我们不能放弃人类的独特优势：对真实世界的理解、对复杂情境的判断、对长期价值的考量。

下次当你看到AI生成的代码中又出现了硬编码的`localhost:23124`时，不要只是想着“我需要修复这个bug”。想想这背后反映了AI的什么局限，以及你可以如何引导它生成更好的代码。

因为最终，最好的代码不是AI写的，也不是人写的，而是人与AI协作写出来的。

---
*本文记录了一次Zotero项目优化中的观察与思考，探讨了AI辅助编程时代人类开发者的新角色。在技术快速演进的今天，保持对技术本质的思考，或许是我们不被技术淹没的最好方式。*